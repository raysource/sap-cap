{
  "key": "test_s03",
  "kind": "bas",
  "w": 1440, "h": 900,
  "window": "odata.test.js - demo-node - SAP Business Application Studio",
  "menus": ["File", "Edit", "Selection", "View", "Go", "Run", "Terminal", "Help"],
  "sideTitle": "EXPLORER",
  "sideW": 258,
  "side": [
    {"label": "DEMO-NODE (WORKSPACE)", "open": true, "items": [
      {"d": 0, "k": "folder", "open": true, "label": "srv", "b": true},
      {"d": 1, "k": "file", "label": "book-order-service.cds"},
      {"d": 1, "k": "file", "label": "book-order-service.js"},
      {"d": 0, "k": "folder", "open": true, "label": "test", "b": true},
      {"d": 1, "k": "file", "label": "odata.test.js", "tag": "M"},
      {"d": 1, "k": "file", "label": "odata-verify.sh"},
      {"d": 1, "k": "file", "label": "dev-proxy.js"},
      {"d": 0, "k": "file", "label": "package.json"},
      {"d": 0, "k": "file", "label": "db.sqlite"}
    ]}
  ],
  "tabs": ["odata.test.js", "book-order-service.js"],
  "activeTab": 0,
  "breadcrumb": "demo-node  ›  test  ›  odata.test.js",
  "editor": {
    "lang": "js",
    "fs": 12,
    "lh": 17,
    "hl": [1, 2, 4, 5, 11, 12, 13, 14, 15, 16, 17, 18],
    "lines": [
      "const cds = require('@sap/cds')",
      "const { GET, POST, expect, defaults, data } = cds.test(__dirname + '/..')",
      "",
      "defaults.auth = { username: 'alice', password: 'alice' }",
      "const SVC = '/odata/v4/book-order'",
      "const fail = (p) => p.then((r) => { throw new Error('expected an error, got ' + r.status) }, (e) => e)",
      "",
      "describe('图书受注服务 —— OData 集成测试（mocha + cds.test）', () => {",
      "  // 本站示范项目把 db 指向 db.sqlite，所以测试前必须把库重置为 CSV 初始状态",
      "  before(async () => { await data.reset() })",
      "",
      "  it('⑫ 草稿：POST 新建订单 → IsActiveEntity=false（未激活）', async () => {",
      "    const draft = await POST(`${SVC}/BookOrders`, { customer_ID: 1000 })",
      "    expect(draft.status).to.equal(201)",
      "    expect(draft.data.IsActiveEntity).to.equal(false)",
      "  })",
      "",
      "  it('⑬ 草稿：draftActivate → 201，触发 before-CREATE 自动编号', async () => {",
      "    const draft = await POST(`${SVC}/BookOrders`, { customer_ID: 1000 })",
      "    const url = `${SVC}/BookOrders(ID=${draft.data.ID},IsActiveEntity=false)/BookOrderService.draftActivate`",
      "    const activated = await POST(url, {})",
      "    expect(activated.status).to.equal(201)",
      "    expect(activated.data.orderNo).to.equal('BO-1005')",
      "  })",
      "",
      "  it('⑯ 绑定动作：重复提交同一张订单 → 409', async () => {",
      "    const orders = await GET(`${SVC}/BookOrders?$select=ID&$filter=orderNo eq 'BO-1001'`)",
      "    const id = orders.data.value[0].ID",
      "    const err = await fail(POST(`${SVC}/BookOrders(ID=${id},IsActiveEntity=true)/submitOrder`, {}))",
      "    expect(err.response.status).to.equal(409)",
      "  })"
    ]
  },
  "panel": {"title": "TERMINAL", "h": 320, "extraTabs": ["PROBLEMS", "OUTPUT"], "lines": [
    "$ npx mocha --reporter spec --timeout 20000 test/odata.test.js",
    "",
    "  …（① ～ ⑪ 省略，仅剩最后 5 条）",
    "    ✔ ⑫ 草稿：POST 新建订单 → IsActiveEntity=false（未激活）",
    "    ✔ ⑬ 草稿：draftActivate → 201，触发 before-CREATE 自动编号",
    "    ✔ ⑭ 绑定动作：空订单提交 → 400",
    "    ✔ ⑮ 绑定动作：BO-1001 提交 → 200，状态迁移为 SUBMITTED",
    "    ✔ ⑯ 绑定动作：重复提交同一张订单 → 409",
    "",
    "",
    "  16 passing (482ms)"
  ]},
  "activity": [{"glyph": "⧉", "active": true}, {"glyph": "⌕"}, {"glyph": "⑂"}, {"glyph": "▷"}, {"glyph": "⌬"}, {"glyph": "⛭"}],
  "status": ["⑂ main", "0 ▲ 0 ●", "Node 22.23.2", "CAP 9.9.3 · cds.test", "UTF-8"],
  "caption": "第 1 层的写法：cds.test 起一个真实服务端，GET/POST 直接打 OData 接口；终端里是同一次运行的真实结果",
  "label": "本地实机运行",
  "src": "本地实测：npx mocha test/odata.test.js",
  "callouts": [
    {"on": "const { GET, POST, expect, defaults, data } = cds.test(__dirname + '/..')", "text": "1. cds.test 起服务并给出 HTTP 帮手"},
    {"on": "  before(async () => { await data.reset() })", "text": "2. 测试前重置库（本站连的是 db.sqlite）"},
    {"on": "    const url = `${SVC}/BookOrders(ID=${draft.data.ID},IsActiveEntity=false)/BookOrderService.draftActivate`", "text": "3. 草稿的真实 URL 形态"},
    {"on": "    expect(err.response.status).to.equal(409)", "text": "4. 负向断言看 err.response.status"},
    {"on": "  16 passing (482ms)", "text": "5. 每加一个 handler 就补一对用例"}
  ]
}
